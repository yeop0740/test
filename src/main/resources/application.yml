spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create # application ????? ?? ?? ??? ? ??? ?? create

logging.level:
  org.hibernate.SQL: debug